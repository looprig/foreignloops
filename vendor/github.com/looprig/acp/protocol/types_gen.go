// Code generated by internal/gen from the pinned ACP schema. DO NOT EDIT.
// Regenerate with: go generate ./protocol/...

package protocol

import (
	"encoding/json"
	"fmt"
)

// AgentAuthCapabilities Authentication-related capabilities supported by the agent.
type AgentAuthCapabilities struct {
	// Whether the agent supports the logout method.
	//
	// Optional. Omitted or `null` both mean the agent does not advertise support.
	// Supplying `{}` means the agent supports the logout method.
	Logout *LogoutCapabilities `json:"logout,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// AgentCapabilities Capabilities supported by the agent.
//
// Advertised during initialization to inform the client about
// available features and content types.
//
// See protocol docs: [Agent Capabilities](https://agentclientprotocol.com/protocol/initialization#agent-capabilities)
type AgentCapabilities struct {
	// Authentication-related capabilities supported by the agent.
	Auth *AgentAuthCapabilities `json:"auth,omitempty"`
	// Whether the agent supports `session/load`.
	LoadSession bool `json:"loadSession,omitempty"`
	// MCP capabilities supported by the agent.
	McpCapabilities *McpCapabilities `json:"mcpCapabilities,omitempty"`
	// Prompt capabilities supported by the agent.
	PromptCapabilities *PromptCapabilities `json:"promptCapabilities,omitempty"`
	// Session lifecycle and prompt capabilities advertised by the agent.
	SessionCapabilities *SessionCapabilities `json:"sessionCapabilities,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// DefaultAgentCapabilities returns AgentCapabilities with every schema-declared default applied.
func DefaultAgentCapabilities() AgentCapabilities {
	return AgentCapabilities{
		LoadSession:        false,
		McpCapabilities:    func() *McpCapabilities { v := DefaultMcpCapabilities(); return &v }(),
		PromptCapabilities: func() *PromptCapabilities { v := DefaultPromptCapabilities(); return &v }(),
	}
}

// Annotations Optional annotations for the client. The client can use annotations to inform how objects are used or displayed
type Annotations struct {
	// Intended recipients for this content, such as the user or assistant.
	Audience []Role `json:"audience,omitempty"`
	// Timestamp indicating when the underlying resource was last modified.
	LastModified *string `json:"lastModified,omitempty"`
	// Relative importance of this content when clients choose what to surface.
	Priority *float64 `json:"priority,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// AudioContent Audio provided to or from an LLM.
type AudioContent struct {
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Base64-encoded media payload.
	Data string `json:"data"`
	// MIME type describing the encoded media payload.
	MimeType string `json:"mimeType"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// AuthMethod Describes an available authentication method.
//
// The `type` field acts as the discriminator in the serialized JSON form.
// When no `type` is present, the method is treated as `agent`.
type AuthMethod = AuthMethodAgent

// AuthMethodAgent Agent handles authentication itself.
//
// This is the default authentication method type.
type AuthMethodAgent struct {
	// Optional description providing more details about this authentication method.
	Description *string `json:"description,omitempty"`
	// Unique identifier for this authentication method.
	ID AuthMethodID `json:"id"`
	// Human-readable name of the authentication method.
	Name string `json:"name"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// AuthMethodID Typed identifier used for auth method values on the wire.
type AuthMethodID string

// AuthenticateRequest Request parameters for the authenticate method.
//
// Specifies which authentication method to use.
type AuthenticateRequest struct {
	// The ID of the authentication method to use.
	// Must be one of the methods advertised in the initialize response.
	MethodID AuthMethodID `json:"methodId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// AuthenticateResponse Response to the `authenticate` method.
type AuthenticateResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// AvailableCommand Information about a command.
type AvailableCommand struct {
	// Human-readable description of what the command does.
	Description string `json:"description"`
	// Input for the command if required
	Input *AvailableCommandInput `json:"input,omitempty"`
	// Command name (e.g., `create_plan`, `research_codebase`).
	Name string `json:"name"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// AvailableCommandInput The input specification for a command.
type AvailableCommandInput = UnstructuredCommandInput

// AvailableCommandsUpdate Available commands are ready or have changed
type AvailableCommandsUpdate struct {
	// Commands the agent can execute
	AvailableCommands []AvailableCommand `json:"availableCommands"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// BlobResourceContents Binary resource contents.
type BlobResourceContents struct {
	// Base64-encoded bytes for a binary resource payload.
	Blob string `json:"blob"`
	// MIME type describing the encoded media payload.
	MimeType *string `json:"mimeType,omitempty"`
	// URI associated with this resource or media payload.
	URI string `json:"uri"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// BooleanConfigOptionCapabilities Capabilities for boolean session configuration options.
//
// Supplying `{}` means the client supports boolean session configuration options.
type BooleanConfigOptionCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// CancelNotification Notification to cancel ongoing operations for a session.
//
// See protocol docs: [Cancellation](https://agentclientprotocol.com/protocol/prompt-turn#cancellation)
type CancelNotification struct {
	// The ID of the session to cancel operations for.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ClientCapabilities Capabilities supported by the client.
//
// Advertised during initialization to inform the agent about
// available features and methods.
//
// See protocol docs: [Client Capabilities](https://agentclientprotocol.com/protocol/initialization#client-capabilities)
type ClientCapabilities struct {
	// File system capabilities supported by the client.
	// Determines which file operations the agent can request.
	Fs *FileSystemCapabilities `json:"fs,omitempty"`
	// Session-related capabilities supported by the client.
	//
	// Optional. Omitted or `null` both mean the client does not advertise any
	// session-related extensions.
	Session *ClientSessionCapabilities `json:"session,omitempty"`
	// Whether the Client support all `terminal/*` methods.
	Terminal bool `json:"terminal,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// DefaultClientCapabilities returns ClientCapabilities with every schema-declared default applied.
func DefaultClientCapabilities() ClientCapabilities {
	return ClientCapabilities{
		Fs:       func() *FileSystemCapabilities { v := DefaultFileSystemCapabilities(); return &v }(),
		Terminal: false,
	}
}

// ClientSessionCapabilities Session-related capabilities supported by the client.
type ClientSessionCapabilities struct {
	// Config option capabilities supported by the client.
	//
	// Omitted or `null` both mean the client does not advertise support for any
	// config option extensions.
	ConfigOptions *SessionConfigOptionsCapabilities `json:"configOptions,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// CloseSessionRequest Request parameters for closing an active session.
//
// If supported, the agent **must** cancel any ongoing work related to the session
// (treat it as if `session/cancel` was called) and then free up any resources
// associated with the session.
//
// Only available if the Agent supports the `sessionCapabilities.close` capability.
type CloseSessionRequest struct {
	// The ID of the session to close.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// CloseSessionResponse Response from closing a session.
type CloseSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ConfigOptionUpdate Session configuration options have been updated.
type ConfigOptionUpdate struct {
	// The full set of configuration options and their current values.
	ConfigOptions []SessionConfigOption `json:"configOptions"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// Content Standard content block (text, images, resources).
type Content struct {
	// The actual content block.
	Content ContentBlock `json:"content"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ContentBlock Content blocks represent displayable information in the Agent Client Protocol.
//
// They provide a structured way to handle various types of user-facing content—whether
// it's text from language models, images for analysis, or embedded resources for context.
//
// Content blocks appear in:
// - User prompts sent via `session/prompt`
// - Language model output streamed through `session/update` notifications
// - Progress updates and results from tool calls
//
// This structure is compatible with the Model Context Protocol (MCP), enabling
// agents to seamlessly forward content from MCP tool outputs without transformation.
//
// See protocol docs: [Content](https://agentclientprotocol.com/protocol/content)
type ContentBlock struct {
	// Text content. May be plain text or formatted with Markdown.
	//
	// All agents MUST support text content blocks in prompts.
	// Clients SHOULD render this text as Markdown.
	Text *TextContent `json:"-"`
	// Images for visual context or analysis.
	//
	// Requires the `image` prompt capability when included in prompts.
	Image *ImageContent `json:"-"`
	// Audio data for transcription or analysis.
	//
	// Requires the `audio` prompt capability when included in prompts.
	Audio *AudioContent `json:"-"`
	// References to resources that the agent can access.
	//
	// All agents MUST support resource links in prompts.
	ResourceLink *ResourceLink `json:"-"`
	// Complete resource contents embedded directly in the message.
	//
	// Preferred for including context as it avoids extra round-trips.
	//
	// Requires the `embeddedContext` prompt capability when included in prompts.
	Resource *EmbeddedResource `json:"-"`
}

// MarshalJSON flattens whichever ContentBlock variant is set, plus the "type"
// discriminator, into one JSON object. It fails if zero or more than one
// variant is set.
func (v ContentBlock) MarshalJSON() ([]byte, error) {
	set := 0
	merged := map[string]json.RawMessage{}
	if v.Text != nil {
		set++
		raw, err := json.Marshal(v.Text)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("text")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.Image != nil {
		set++
		raw, err := json.Marshal(v.Image)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("image")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.Audio != nil {
		set++
		raw, err := json.Marshal(v.Audio)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("audio")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.ResourceLink != nil {
		set++
		raw, err := json.Marshal(v.ResourceLink)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("resource_link")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.Resource != nil {
		set++
		raw, err := json.Marshal(v.Resource)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("resource")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if set == 0 {
		return nil, fmt.Errorf("ContentBlock: no variant set")
	}
	if set > 1 {
		return nil, fmt.Errorf("ContentBlock: more than one variant set")
	}
	return json.Marshal(merged)
}

// UnmarshalJSON dispatches on the "type" discriminator. A missing or
// unrecognized value is rejected.
func (v *ContentBlock) UnmarshalJSON(data []byte) error {
	var tag struct {
		Value *string `json:"type"`
	}
	if err := json.Unmarshal(data, &tag); err != nil {
		return fmt.Errorf("ContentBlock: %w", err)
	}
	*v = ContentBlock{}
	switch {
	case tag.Value != nil && *tag.Value == "text":
		var payload TextContent
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("ContentBlock: text: %w", err)
		}
		v.Text = &payload
	case tag.Value != nil && *tag.Value == "image":
		var payload ImageContent
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("ContentBlock: image: %w", err)
		}
		v.Image = &payload
	case tag.Value != nil && *tag.Value == "audio":
		var payload AudioContent
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("ContentBlock: audio: %w", err)
		}
		v.Audio = &payload
	case tag.Value != nil && *tag.Value == "resource_link":
		var payload ResourceLink
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("ContentBlock: resource_link: %w", err)
		}
		v.ResourceLink = &payload
	case tag.Value != nil && *tag.Value == "resource":
		var payload EmbeddedResource
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("ContentBlock: resource: %w", err)
		}
		v.Resource = &payload
	case tag.Value == nil:
		return fmt.Errorf("ContentBlock: missing discriminator field type")
	default:
		return fmt.Errorf("ContentBlock: unrecognized type %q", *tag.Value)
	}
	return nil
}

// ContentChunk A streamed item of content
type ContentChunk struct {
	// A single item of content
	Content ContentBlock `json:"content"`
	// A unique identifier for the message this chunk belongs to.
	//
	// All chunks belonging to the same message share the same `messageId`.
	// A change in `messageId` indicates a new message has started.
	MessageID *MessageID `json:"messageId,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// Cost Cost information for a session.
type Cost struct {
	// Total cumulative cost for session.
	Amount float64 `json:"amount"`
	// ISO 4217 currency code (e.g., "USD", "EUR").
	Currency string `json:"currency"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// CreateTerminalRequest Request to create a new terminal and execute a command.
type CreateTerminalRequest struct {
	// Array of command arguments.
	Args []string `json:"args,omitempty"`
	// The command to execute.
	Command string `json:"command"`
	// Working directory for the command. Must be an absolute path.
	Cwd *string `json:"cwd,omitempty"`
	// Environment variables for the command.
	Env []EnvVariable `json:"env,omitempty"`
	// Maximum number of output bytes to retain.
	//
	// When the limit is exceeded, the Client truncates from the beginning of the output
	// to stay within the limit.
	//
	// The Client MUST ensure truncation happens at a character boundary to maintain valid
	// string output, even if this means the retained output is slightly less than the
	// specified limit.
	OutputByteLimit *uint64 `json:"outputByteLimit,omitempty"`
	// The session ID for this request.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// CreateTerminalResponse Response containing the ID of the created terminal.
type CreateTerminalResponse struct {
	// The unique identifier for the created terminal.
	TerminalID TerminalID `json:"terminalId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// CurrentModeUpdate The current mode of the session has changed
//
// See protocol docs: [Session Modes](https://agentclientprotocol.com/protocol/session-modes)
type CurrentModeUpdate struct {
	// The ID of the current mode
	CurrentModeID SessionModeID `json:"currentModeId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// DeleteSessionRequest Request parameters for deleting an existing session from `session/list`.
//
// Only available if the Agent supports the `sessionCapabilities.delete` capability.
type DeleteSessionRequest struct {
	// The ID of the session to delete.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// DeleteSessionResponse Response from deleting a session.
type DeleteSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// Diff A diff representing file modifications.
//
// Shows changes to files in a format suitable for display in the client UI.
//
// See protocol docs: [Content](https://agentclientprotocol.com/protocol/tool-calls#content)
type Diff struct {
	// The new content after modification.
	NewText string `json:"newText"`
	// The original content (None for new files).
	OldText *string `json:"oldText,omitempty"`
	// The absolute file path being modified.
	Path string `json:"path"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// EmbeddedResource The contents of a resource, embedded into a prompt or tool call result.
type EmbeddedResource struct {
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Embedded resource payload, either text or binary data.
	Resource EmbeddedResourceResource `json:"resource"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// EmbeddedResourceResource Resource content that can be embedded in a message.
type EmbeddedResourceResource struct {
	// Text resource contents embedded directly in the message.
	TextResourceContents *TextResourceContents `json:"-"`
	// Binary resource contents embedded directly in the message.
	BlobResourceContents *BlobResourceContents `json:"-"`
}

// MarshalJSON marshals whichever EmbeddedResourceResource variant is set. It fails if zero or
// more than one variant is set.
func (v EmbeddedResourceResource) MarshalJSON() ([]byte, error) {
	set := 0
	var out []byte
	if v.TextResourceContents != nil {
		set++
		var err error
		out, err = json.Marshal(v.TextResourceContents)
		if err != nil {
			return nil, err
		}
	}
	if v.BlobResourceContents != nil {
		set++
		var err error
		out, err = json.Marshal(v.BlobResourceContents)
		if err != nil {
			return nil, err
		}
	}
	if set == 0 {
		return nil, fmt.Errorf("EmbeddedResourceResource: no variant set")
	}
	if set > 1 {
		return nil, fmt.Errorf("EmbeddedResourceResource: more than one variant set")
	}
	return out, nil
}

// UnmarshalJSON tells EmbeddedResourceResource's variants apart by which one's required fields
// are present on the wire object (this union has no discriminator tag).
func (v *EmbeddedResourceResource) UnmarshalJSON(data []byte) error {
	var probe map[string]json.RawMessage
	if err := json.Unmarshal(data, &probe); err != nil {
		return fmt.Errorf("EmbeddedResourceResource: %w", err)
	}
	*v = EmbeddedResourceResource{}
	matches := 0
	if probe["text"] != nil && probe["uri"] != nil {
		var payload TextResourceContents
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("EmbeddedResourceResource: TextResourceContents: %w", err)
		}
		v.TextResourceContents = &payload
		matches++
	}
	if probe["blob"] != nil && probe["uri"] != nil {
		var payload BlobResourceContents
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("EmbeddedResourceResource: BlobResourceContents: %w", err)
		}
		v.BlobResourceContents = &payload
		matches++
	}
	if matches == 0 {
		return fmt.Errorf("EmbeddedResourceResource: no variant's required fields are present")
	}
	if matches > 1 {
		return fmt.Errorf("EmbeddedResourceResource: ambiguous, multiple variants match")
	}
	return nil
}

// EnvVariable An environment variable to set when launching an MCP server.
type EnvVariable struct {
	// The name of the environment variable.
	Name string `json:"name"`
	// The value to set for the environment variable.
	Value string `json:"value"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ErrorCode Predefined error codes for common JSON-RPC and ACP-specific errors.
//
// These codes follow the JSON-RPC 2.0 specification for standard errors
// and use the reserved range (-32000 to -32099) for protocol-specific errors.
type ErrorCode int32

const (
	// **Parse error**: Invalid JSON was received by the server.
	// An error occurred on the server while parsing the JSON text.
	ErrorCodeParseError ErrorCode = -32700
	// **Invalid request**: The JSON sent is not a valid Request object.
	ErrorCodeInvalidRequest ErrorCode = -32600
	// **Method not found**: The method does not exist or is not available.
	ErrorCodeMethodNotFound ErrorCode = -32601
	// **Invalid params**: Invalid method parameter(s).
	ErrorCodeInvalidParams ErrorCode = -32602
	// **Internal error**: Internal JSON-RPC error.
	// Reserved for implementation-defined server errors.
	ErrorCodeInternalError ErrorCode = -32603
	// **Request cancelled**: Execution of the method was aborted either due to a cancellation request from the caller or
	// because of resource constraints or shutdown.
	ErrorCodeRequestCancelled ErrorCode = -32800
	// **Authentication required**: Authentication is required before this operation can be performed.
	ErrorCodeAuthenticationRequired ErrorCode = -32000
	// **Resource not found**: A given resource, such as a file, was not found.
	ErrorCodeResourceNotFound ErrorCode = -32002
)

// FileSystemCapabilities File system capabilities that a client may support.
//
// See protocol docs: [FileSystem](https://agentclientprotocol.com/protocol/initialization#filesystem)
type FileSystemCapabilities struct {
	// Whether the Client supports `fs/read_text_file` requests.
	ReadTextFile bool `json:"readTextFile,omitempty"`
	// Whether the Client supports `fs/write_text_file` requests.
	WriteTextFile bool `json:"writeTextFile,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// DefaultFileSystemCapabilities returns FileSystemCapabilities with every schema-declared default applied.
func DefaultFileSystemCapabilities() FileSystemCapabilities {
	return FileSystemCapabilities{
		ReadTextFile:  false,
		WriteTextFile: false,
	}
}

// HTTPHeader An HTTP header to set when making requests to the MCP server.
type HTTPHeader struct {
	// The name of the HTTP header.
	Name string `json:"name"`
	// The value to set for the HTTP header.
	Value string `json:"value"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ImageContent An image provided to or from an LLM.
type ImageContent struct {
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Base64-encoded media payload.
	Data string `json:"data"`
	// MIME type describing the encoded media payload.
	MimeType string `json:"mimeType"`
	// URI associated with this resource or media payload.
	URI *string `json:"uri,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// Implementation Metadata about the implementation of the client or agent.
// Describes the name and version of an ACP implementation, with an optional
// title for UI representation.
type Implementation struct {
	// Intended for programmatic or logical use, but can be used as a display
	// name fallback if title isn’t present.
	Name string `json:"name"`
	// Intended for UI and end-user contexts — optimized to be human-readable
	// and easily understood.
	//
	// If not provided, the name should be used for display.
	Title *string `json:"title,omitempty"`
	// Version of the implementation. Can be displayed to the user or used
	// for debugging or metrics purposes. (e.g. "1.0.0").
	Version string `json:"version"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// InitializeRequest Request parameters for the initialize method.
//
// Sent by the client to establish connection and negotiate capabilities.
//
// See protocol docs: [Initialization](https://agentclientprotocol.com/protocol/initialization)
type InitializeRequest struct {
	// Capabilities supported by the client.
	ClientCapabilities *ClientCapabilities `json:"clientCapabilities,omitempty"`
	// Information about the Client name and version sent to the Agent.
	//
	// Note: in future versions of the protocol, this will be required.
	ClientInfo *Implementation `json:"clientInfo,omitempty"`
	// The latest protocol version supported by the client.
	ProtocolVersion ProtocolVersion `json:"protocolVersion"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// InitializeResponse Response to the `initialize` method.
//
// Contains the negotiated protocol version and agent capabilities.
//
// See protocol docs: [Initialization](https://agentclientprotocol.com/protocol/initialization)
type InitializeResponse struct {
	// Capabilities supported by the agent.
	AgentCapabilities *AgentCapabilities `json:"agentCapabilities,omitempty"`
	// Information about the Agent name and version sent to the Client.
	//
	// Note: in future versions of the protocol, this will be required.
	AgentInfo *Implementation `json:"agentInfo,omitempty"`
	// Authentication methods supported by the agent.
	AuthMethods []AuthMethod `json:"authMethods,omitempty"`
	// The protocol version the client specified if supported by the agent,
	// or the latest protocol version supported by the agent.
	//
	// The client should disconnect, if it doesn't support this version.
	ProtocolVersion ProtocolVersion `json:"protocolVersion"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// KillTerminalRequest Request to kill a terminal without releasing it.
type KillTerminalRequest struct {
	// The session ID for this request.
	SessionID SessionID `json:"sessionId"`
	// The ID of the terminal to kill.
	TerminalID TerminalID `json:"terminalId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// KillTerminalResponse Response to `terminal/kill` method
type KillTerminalResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ListSessionsRequest Request parameters for listing existing sessions.
//
// Only available if the Agent supports the `sessionCapabilities.list` capability.
type ListSessionsRequest struct {
	// Opaque cursor token from a previous response's nextCursor field for cursor-based pagination
	Cursor *string `json:"cursor,omitempty"`
	// Filter sessions by working directory. Must be an absolute path.
	Cwd *string `json:"cwd,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ListSessionsResponse Response from listing sessions.
type ListSessionsResponse struct {
	// Opaque cursor token. If present, pass this in the next request's cursor parameter
	// to fetch the next page. If absent, there are no more results.
	NextCursor *string `json:"nextCursor,omitempty"`
	// Array of session information objects
	Sessions []SessionInfo `json:"sessions"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// LoadSessionRequest Request parameters for loading an existing session.
//
// Only available if the Agent supports the `loadSession` capability.
//
// See protocol docs: [Loading Sessions](https://agentclientprotocol.com/protocol/session-setup#loading-sessions)
type LoadSessionRequest struct {
	// Additional workspace roots to activate for this session. Each path must be absolute.
	//
	// When omitted or empty, no additional roots are activated. When non-empty,
	// this is the complete resulting additional-root list for the loaded
	// session. It may differ from any previously used or reported list as long as
	// the request `cwd` matches the session's `cwd`.
	AdditionalDirectories []string `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd string `json:"cwd"`
	// List of MCP servers to connect to for this session.
	McpServers []McpServer `json:"mcpServers"`
	// The ID of the session to load.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// LoadSessionResponse Response from loading an existing session.
type LoadSessionResponse struct {
	// Initial session configuration options if supported by the Agent.
	ConfigOptions []SessionConfigOption `json:"configOptions,omitempty"`
	// Initial mode state if supported by the Agent
	//
	// See protocol docs: [Session Modes](https://agentclientprotocol.com/protocol/session-modes)
	Modes *SessionModeState `json:"modes,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// LogoutCapabilities Logout capabilities supported by the agent.
//
// Supplying `{}` means the agent supports the logout method.
type LogoutCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// LogoutRequest Request parameters for the logout method.
//
// Terminates the current authenticated session.
type LogoutRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// LogoutResponse Response to the `logout` method.
type LogoutResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// McpCapabilities MCP capabilities supported by the agent
type McpCapabilities struct {
	// Agent supports [`McpServer::Http`].
	HTTP bool `json:"http,omitempty"`
	// Agent supports [`McpServer::Sse`].
	Sse bool `json:"sse,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// DefaultMcpCapabilities returns McpCapabilities with every schema-declared default applied.
func DefaultMcpCapabilities() McpCapabilities {
	return McpCapabilities{
		HTTP: false,
		Sse:  false,
	}
}

// McpServer Configuration for connecting to an MCP (Model Context Protocol) server.
//
// MCP servers provide tools and context that the agent can use when
// processing prompts.
//
// See protocol docs: [MCP Servers](https://agentclientprotocol.com/protocol/session-setup#mcp-servers)
type McpServer struct {
	// HTTP transport configuration
	//
	// Only available when the Agent capabilities indicate `mcp_capabilities.http` is `true`.
	HTTP *McpServerHTTP `json:"-"`
	// SSE transport configuration
	//
	// Only available when the Agent capabilities indicate `mcp_capabilities.sse` is `true`.
	Sse *McpServerSse `json:"-"`
	// Stdio transport configuration
	//
	// All Agents MUST support this transport.
	Stdio *McpServerStdio `json:"-"`
}

// MarshalJSON flattens whichever McpServer variant is set, plus the "type"
// discriminator, into one JSON object. It fails if zero or more than one
// variant is set.
func (v McpServer) MarshalJSON() ([]byte, error) {
	set := 0
	merged := map[string]json.RawMessage{}
	if v.HTTP != nil {
		set++
		raw, err := json.Marshal(v.HTTP)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("http")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.Sse != nil {
		set++
		raw, err := json.Marshal(v.Sse)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("sse")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.Stdio != nil {
		set++
		raw, err := json.Marshal(v.Stdio)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
	}
	if set == 0 {
		return nil, fmt.Errorf("McpServer: no variant set")
	}
	if set > 1 {
		return nil, fmt.Errorf("McpServer: more than one variant set")
	}
	return json.Marshal(merged)
}

// UnmarshalJSON dispatches on the "type" discriminator. A missing or
// unrecognized value falls back to Stdio, matching the pinned schema.
func (v *McpServer) UnmarshalJSON(data []byte) error {
	var tag struct {
		Value *string `json:"type"`
	}
	if err := json.Unmarshal(data, &tag); err != nil {
		return fmt.Errorf("McpServer: %w", err)
	}
	*v = McpServer{}
	switch {
	case tag.Value != nil && *tag.Value == "http":
		var payload McpServerHTTP
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("McpServer: http: %w", err)
		}
		v.HTTP = &payload
	case tag.Value != nil && *tag.Value == "sse":
		var payload McpServerSse
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("McpServer: sse: %w", err)
		}
		v.Sse = &payload
	default:
		var payload McpServerStdio
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("McpServer: %w", err)
		}
		v.Stdio = &payload
	}
	return nil
}

// McpServerHTTP HTTP transport configuration for MCP.
type McpServerHTTP struct {
	// HTTP headers to set when making requests to the MCP server.
	Headers []HTTPHeader `json:"headers"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
	// URL to the MCP server.
	URL string `json:"url"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// McpServerSse SSE transport configuration for MCP.
type McpServerSse struct {
	// HTTP headers to set when making requests to the MCP server.
	Headers []HTTPHeader `json:"headers"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
	// URL to the MCP server.
	URL string `json:"url"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// McpServerStdio Stdio transport configuration for MCP.
type McpServerStdio struct {
	// Command-line arguments to pass to the MCP server.
	Args []string `json:"args"`
	// Absolute path to the MCP server executable.
	Command string `json:"command"`
	// Environment variables to set when launching the MCP server.
	Env []EnvVariable `json:"env"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// MessageID Unique identifier for a message within a session.
type MessageID string

// NewSessionRequest Request parameters for creating a new session.
//
// See protocol docs: [Creating a Session](https://agentclientprotocol.com/protocol/session-setup#creating-a-session)
type NewSessionRequest struct {
	// Additional workspace roots for this session. Each path must be absolute.
	//
	// These expand the session's filesystem scope without changing `cwd`, which
	// remains the base for relative paths. When omitted or empty, no
	// additional roots are activated for the new session.
	AdditionalDirectories []string `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd string `json:"cwd"`
	// List of MCP (Model Context Protocol) servers the agent should connect to.
	McpServers []McpServer `json:"mcpServers"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// NewSessionResponse Response from creating a new session.
//
// See protocol docs: [Creating a Session](https://agentclientprotocol.com/protocol/session-setup#creating-a-session)
type NewSessionResponse struct {
	// Initial session configuration options if supported by the Agent.
	ConfigOptions []SessionConfigOption `json:"configOptions,omitempty"`
	// Initial mode state if supported by the Agent
	//
	// See protocol docs: [Session Modes](https://agentclientprotocol.com/protocol/session-modes)
	Modes *SessionModeState `json:"modes,omitempty"`
	// Unique identifier for the created session.
	//
	// Used in all subsequent requests for this conversation.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// PermissionOption An option presented to the user when requesting permission.
type PermissionOption struct {
	// Hint about the nature of this permission option.
	Kind PermissionOptionKind `json:"kind"`
	// Human-readable label to display to the user.
	Name string `json:"name"`
	// Unique identifier for this permission option.
	OptionID PermissionOptionID `json:"optionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// PermissionOptionID Unique identifier for a permission option.
type PermissionOptionID string

// PermissionOptionKind The type of permission option being presented to the user.
//
// Helps clients choose appropriate icons and UI treatment.
type PermissionOptionKind string

const (
	// Allow this operation only this time.
	PermissionOptionKindAllowOnce PermissionOptionKind = "allow_once"
	// Allow this operation and remember the choice.
	PermissionOptionKindAllowAlways PermissionOptionKind = "allow_always"
	// Reject this operation only this time.
	PermissionOptionKindRejectOnce PermissionOptionKind = "reject_once"
	// Reject this operation and remember the choice.
	PermissionOptionKindRejectAlways PermissionOptionKind = "reject_always"
)

// UnmarshalJSON rejects any PermissionOptionKind value that is not one of the named
// PermissionOptionKind constants: this enum is closed in the pinned schema.
func (v *PermissionOptionKind) UnmarshalJSON(data []byte) error {
	var raw string
	if err := json.Unmarshal(data, &raw); err != nil {
		return fmt.Errorf("PermissionOptionKind: %w", err)
	}
	switch PermissionOptionKind(raw) {
	case PermissionOptionKindAllowOnce, PermissionOptionKindAllowAlways, PermissionOptionKindRejectOnce, PermissionOptionKindRejectAlways:
		*v = PermissionOptionKind(raw)
		return nil
	default:
		return fmt.Errorf("PermissionOptionKind: unrecognized value %v", raw)
	}
}

// Plan An execution plan for accomplishing complex tasks.
//
// Plans consist of multiple entries representing individual tasks or goals.
// Agents report plans to clients to provide visibility into their execution strategy.
// Plans can evolve during execution as the agent discovers new requirements or completes tasks.
//
// See protocol docs: [Agent Plan](https://agentclientprotocol.com/protocol/agent-plan)
type Plan struct {
	// The list of tasks to be accomplished.
	//
	// When updating a plan, the agent must send a complete list of all entries
	// with their current status. The client replaces the entire plan with each update.
	Entries []PlanEntry `json:"entries"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// PlanEntry A single entry in the execution plan.
//
// Represents a task or goal that the assistant intends to accomplish
// as part of fulfilling the user's request.
// See protocol docs: [Plan Entries](https://agentclientprotocol.com/protocol/agent-plan#plan-entries)
type PlanEntry struct {
	// Human-readable description of what this task aims to accomplish.
	Content string `json:"content"`
	// The relative importance of this task.
	// Used to indicate which tasks are most critical to the overall goal.
	Priority PlanEntryPriority `json:"priority"`
	// Current execution status of this task.
	Status PlanEntryStatus `json:"status"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// PlanEntryPriority Priority levels for plan entries.
//
// Used to indicate the relative importance or urgency of different
// tasks in the execution plan.
// See protocol docs: [Plan Entries](https://agentclientprotocol.com/protocol/agent-plan#plan-entries)
type PlanEntryPriority string

const (
	// High priority task - critical to the overall goal.
	PlanEntryPriorityHigh PlanEntryPriority = "high"
	// Medium priority task - important but not critical.
	PlanEntryPriorityMedium PlanEntryPriority = "medium"
	// Low priority task - nice to have but not essential.
	PlanEntryPriorityLow PlanEntryPriority = "low"
)

// UnmarshalJSON rejects any PlanEntryPriority value that is not one of the named
// PlanEntryPriority constants: this enum is closed in the pinned schema.
func (v *PlanEntryPriority) UnmarshalJSON(data []byte) error {
	var raw string
	if err := json.Unmarshal(data, &raw); err != nil {
		return fmt.Errorf("PlanEntryPriority: %w", err)
	}
	switch PlanEntryPriority(raw) {
	case PlanEntryPriorityHigh, PlanEntryPriorityMedium, PlanEntryPriorityLow:
		*v = PlanEntryPriority(raw)
		return nil
	default:
		return fmt.Errorf("PlanEntryPriority: unrecognized value %v", raw)
	}
}

// PlanEntryStatus Status of a plan entry in the execution flow.
//
// Tracks the lifecycle of each task from planning through completion.
// See protocol docs: [Plan Entries](https://agentclientprotocol.com/protocol/agent-plan#plan-entries)
type PlanEntryStatus string

const (
	// The task has not started yet.
	PlanEntryStatusPending PlanEntryStatus = "pending"
	// The task is currently being worked on.
	PlanEntryStatusInProgress PlanEntryStatus = "in_progress"
	// The task has been successfully completed.
	PlanEntryStatusCompleted PlanEntryStatus = "completed"
)

// UnmarshalJSON rejects any PlanEntryStatus value that is not one of the named
// PlanEntryStatus constants: this enum is closed in the pinned schema.
func (v *PlanEntryStatus) UnmarshalJSON(data []byte) error {
	var raw string
	if err := json.Unmarshal(data, &raw); err != nil {
		return fmt.Errorf("PlanEntryStatus: %w", err)
	}
	switch PlanEntryStatus(raw) {
	case PlanEntryStatusPending, PlanEntryStatusInProgress, PlanEntryStatusCompleted:
		*v = PlanEntryStatus(raw)
		return nil
	default:
		return fmt.Errorf("PlanEntryStatus: unrecognized value %v", raw)
	}
}

// PromptCapabilities Prompt capabilities supported by the agent in `session/prompt` requests.
//
// Baseline agent functionality requires support for [`ContentBlock::Text`]
// and [`ContentBlock::ResourceLink`] in prompt requests.
//
// Other variants must be explicitly opted in to.
// Capabilities for different types of content in prompt requests.
//
// Indicates which content types beyond the baseline (text and resource links)
// the agent can process.
//
// See protocol docs: [Prompt Capabilities](https://agentclientprotocol.com/protocol/initialization#prompt-capabilities)
type PromptCapabilities struct {
	// Agent supports [`ContentBlock::Audio`].
	Audio bool `json:"audio,omitempty"`
	// Agent supports embedded context in `session/prompt` requests.
	//
	// When enabled, the Client is allowed to include [`ContentBlock::Resource`]
	// in prompt requests for pieces of context that are referenced in the message.
	EmbeddedContext bool `json:"embeddedContext,omitempty"`
	// Agent supports [`ContentBlock::Image`].
	Image bool `json:"image,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// DefaultPromptCapabilities returns PromptCapabilities with every schema-declared default applied.
func DefaultPromptCapabilities() PromptCapabilities {
	return PromptCapabilities{
		Audio:           false,
		EmbeddedContext: false,
		Image:           false,
	}
}

// PromptRequest Request parameters for sending a user prompt to the agent.
//
// Contains the user's message and any additional context.
//
// See protocol docs: [User Message](https://agentclientprotocol.com/protocol/prompt-turn#1-user-message)
type PromptRequest struct {
	// The blocks of content that compose the user's message.
	//
	// As a baseline, the Agent MUST support [`ContentBlock::Text`] and [`ContentBlock::ResourceLink`],
	// while other variants are optionally enabled via [`PromptCapabilities`].
	//
	// The Client MUST adapt its interface according to [`PromptCapabilities`].
	//
	// The client MAY include referenced pieces of context as either
	// [`ContentBlock::Resource`] or [`ContentBlock::ResourceLink`].
	//
	// When available, [`ContentBlock::Resource`] is preferred
	// as it avoids extra round-trips and allows the message to include
	// pieces of context from sources the agent may not have access to.
	Prompt []ContentBlock `json:"prompt"`
	// The ID of the session to send this user message to
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// PromptResponse Response from processing a user prompt.
//
// See protocol docs: [Check for Completion](https://agentclientprotocol.com/protocol/prompt-turn#4-check-for-completion)
type PromptResponse struct {
	// Indicates why the agent stopped processing the turn.
	StopReason StopReason `json:"stopReason"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ProtocolVersion Protocol version identifier.
//
// This version is only bumped for breaking changes.
// Non-breaking changes should be introduced via capabilities.
type ProtocolVersion uint16

// ReadTextFileRequest Request to read content from a text file.
//
// Only available if the client supports the `fs.readTextFile` capability.
type ReadTextFileRequest struct {
	// Maximum number of lines to read.
	Limit *uint32 `json:"limit,omitempty"`
	// Line number to start reading from (1-based).
	Line *uint32 `json:"line,omitempty"`
	// Absolute path to the file to read.
	Path string `json:"path"`
	// The session ID for this request.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ReadTextFileResponse Response containing the contents of a text file.
type ReadTextFileResponse struct {
	// Content payload returned by this response.
	Content string `json:"content"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ReleaseTerminalRequest Request to release a terminal and free its resources.
type ReleaseTerminalRequest struct {
	// The session ID for this request.
	SessionID SessionID `json:"sessionId"`
	// The ID of the terminal to release.
	TerminalID TerminalID `json:"terminalId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ReleaseTerminalResponse Response to terminal/release method
type ReleaseTerminalResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// RequestPermissionOutcome The outcome of a permission request.
type RequestPermissionOutcome struct {
	// The prompt turn was cancelled before the user responded.
	//
	// When a client sends a `session/cancel` notification to cancel an ongoing
	// prompt turn, it MUST respond to all pending `session/request_permission`
	// requests with this `Cancelled` outcome.
	//
	// See protocol docs: [Cancellation](https://agentclientprotocol.com/protocol/prompt-turn#cancellation)
	Cancelled *struct{} `json:"-"`
	// The user selected one of the provided options.
	Selected *SelectedPermissionOutcome `json:"-"`
}

// MarshalJSON flattens whichever RequestPermissionOutcome variant is set, plus the "outcome"
// discriminator, into one JSON object. It fails if zero or more than one
// variant is set.
func (v RequestPermissionOutcome) MarshalJSON() ([]byte, error) {
	set := 0
	merged := map[string]json.RawMessage{}
	if v.Cancelled != nil {
		set++
		raw, err := json.Marshal(v.Cancelled)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("cancelled")
		if err != nil {
			return nil, err
		}
		merged["outcome"] = tagBytes
	}
	if v.Selected != nil {
		set++
		raw, err := json.Marshal(v.Selected)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("selected")
		if err != nil {
			return nil, err
		}
		merged["outcome"] = tagBytes
	}
	if set == 0 {
		return nil, fmt.Errorf("RequestPermissionOutcome: no variant set")
	}
	if set > 1 {
		return nil, fmt.Errorf("RequestPermissionOutcome: more than one variant set")
	}
	return json.Marshal(merged)
}

// UnmarshalJSON dispatches on the "outcome" discriminator. A missing or
// unrecognized value is rejected.
func (v *RequestPermissionOutcome) UnmarshalJSON(data []byte) error {
	var tag struct {
		Value *string `json:"outcome"`
	}
	if err := json.Unmarshal(data, &tag); err != nil {
		return fmt.Errorf("RequestPermissionOutcome: %w", err)
	}
	*v = RequestPermissionOutcome{}
	switch {
	case tag.Value != nil && *tag.Value == "cancelled":
		var payload struct{}
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("RequestPermissionOutcome: cancelled: %w", err)
		}
		v.Cancelled = &payload
	case tag.Value != nil && *tag.Value == "selected":
		var payload SelectedPermissionOutcome
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("RequestPermissionOutcome: selected: %w", err)
		}
		v.Selected = &payload
	case tag.Value == nil:
		return fmt.Errorf("RequestPermissionOutcome: missing discriminator field outcome")
	default:
		return fmt.Errorf("RequestPermissionOutcome: unrecognized outcome %q", *tag.Value)
	}
	return nil
}

// RequestPermissionRequest Request for user permission to execute a tool call.
//
// Sent when the agent needs authorization before performing a sensitive operation.
//
// See protocol docs: [Requesting Permission](https://agentclientprotocol.com/protocol/tool-calls#requesting-permission)
type RequestPermissionRequest struct {
	// Available permission options for the user to choose from.
	Options []PermissionOption `json:"options"`
	// The session ID for this request.
	SessionID SessionID `json:"sessionId"`
	// Details about the tool call requiring permission.
	ToolCall ToolCallUpdate `json:"toolCall"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// RequestPermissionResponse Response to a permission request.
type RequestPermissionResponse struct {
	// The user's decision on the permission request.
	Outcome RequestPermissionOutcome `json:"outcome"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ResourceLink A resource that the server is capable of reading, included in a prompt or tool call result.
type ResourceLink struct {
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Optional human-readable details shown with this protocol object.
	Description *string `json:"description,omitempty"`
	// MIME type describing the encoded media payload.
	MimeType *string `json:"mimeType,omitempty"`
	// Human-readable name shown for this protocol object.
	Name string `json:"name"`
	// Optional size of the linked resource in bytes, if known.
	Size *int64 `json:"size,omitempty"`
	// Optional display title for end-user UI.
	Title *string `json:"title,omitempty"`
	// URI associated with this resource or media payload.
	URI string `json:"uri"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ResumeSessionRequest Request parameters for resuming an existing session.
//
// Resumes an existing session without returning previous messages (unlike `session/load`).
// This is useful for agents that can resume sessions but don't implement full session loading.
//
// Only available if the Agent supports the `sessionCapabilities.resume` capability.
type ResumeSessionRequest struct {
	// Additional workspace roots to activate for this session. Each path must be absolute.
	//
	// When omitted or empty, no additional roots are activated. When non-empty,
	// this is the complete resulting additional-root list for the resumed
	// session. It may differ from any previously used or reported list as long as
	// the request `cwd` matches the session's `cwd`.
	AdditionalDirectories []string `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd string `json:"cwd"`
	// List of MCP servers to connect to for this session.
	McpServers []McpServer `json:"mcpServers,omitempty"`
	// The ID of the session to resume.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ResumeSessionResponse Response from resuming an existing session.
type ResumeSessionResponse struct {
	// Initial session configuration options if supported by the Agent.
	ConfigOptions []SessionConfigOption `json:"configOptions,omitempty"`
	// Initial mode state if supported by the Agent
	//
	// See protocol docs: [Session Modes](https://agentclientprotocol.com/protocol/session-modes)
	Modes *SessionModeState `json:"modes,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// Role The sender or recipient of messages and data in a conversation.
type Role string

const (
	// The assistant side of a conversation.
	RoleAssistant Role = "assistant"
	// The user side of a conversation.
	RoleUser Role = "user"
)

// UnmarshalJSON rejects any Role value that is not one of the named
// Role constants: this enum is closed in the pinned schema.
func (v *Role) UnmarshalJSON(data []byte) error {
	var raw string
	if err := json.Unmarshal(data, &raw); err != nil {
		return fmt.Errorf("Role: %w", err)
	}
	switch Role(raw) {
	case RoleAssistant, RoleUser:
		*v = Role(raw)
		return nil
	default:
		return fmt.Errorf("Role: unrecognized value %v", raw)
	}
}

// SelectedPermissionOutcome The user selected one of the provided options.
type SelectedPermissionOutcome struct {
	// The ID of the option the user selected.
	OptionID PermissionOptionID `json:"optionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionAdditionalDirectoriesCapabilities Capabilities for additional session directories support.
//
// Supplying `{}` means the agent supports the `additionalDirectories` field on
// supported session lifecycle requests. Agents that also support
// `session/list` may return `SessionInfo.additionalDirectories` to report the
// complete ordered additional-root list associated with a listed session.
type SessionAdditionalDirectoriesCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionCapabilities Session capabilities supported by the agent.
//
// As a baseline, all Agents **MUST** support `session/new`, `session/prompt`, `session/cancel`, and `session/update`.
//
// Optionally, they **MAY** support other session methods and notifications by specifying additional capabilities.
//
// Note: `session/load` is still handled by the top-level `load_session` capability. This will be unified in future versions of the protocol.
//
// See protocol docs: [Session Capabilities](https://agentclientprotocol.com/protocol/initialization#session-capabilities)
type SessionCapabilities struct {
	// Whether the agent supports `additionalDirectories` on supported session lifecycle requests.
	//
	// Optional. Omitted or `null` both mean the agent does not advertise support.
	// Supplying `{}` means the agent supports `additionalDirectories` on
	// supported session lifecycle requests.
	//
	// Agents that also support `session/list` may return
	// `SessionInfo.additionalDirectories` to report the complete ordered
	// additional-root list associated with a listed session.
	AdditionalDirectories *SessionAdditionalDirectoriesCapabilities `json:"additionalDirectories,omitempty"`
	// Whether the agent supports `session/close`.
	//
	// Optional. Omitted or `null` both mean the agent does not advertise support.
	// Supplying `{}` means the agent supports closing sessions.
	Close *SessionCloseCapabilities `json:"close,omitempty"`
	// Whether the agent supports `session/delete`.
	//
	// Optional. Omitted or `null` both mean the agent does not advertise support.
	// Supplying `{}` means the agent supports deleting sessions from `session/list`.
	Delete *SessionDeleteCapabilities `json:"delete,omitempty"`
	// Whether the agent supports `session/list`.
	//
	// Optional. Omitted or `null` both mean the agent does not advertise support.
	// Supplying `{}` means the agent supports listing sessions.
	List *SessionListCapabilities `json:"list,omitempty"`
	// Whether the agent supports `session/resume`.
	//
	// Optional. Omitted or `null` both mean the agent does not advertise support.
	// Supplying `{}` means the agent supports resuming sessions.
	Resume *SessionResumeCapabilities `json:"resume,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionCloseCapabilities Capabilities for the `session/close` method.
//
// Supplying `{}` means the agent supports closing sessions.
type SessionCloseCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionConfigBoolean A boolean on/off toggle session configuration option payload.
type SessionConfigBoolean struct {
	// The current value of the boolean option.
	CurrentValue bool `json:"currentValue"`
}

// SessionConfigGroupID Unique identifier for a session configuration option value group.
type SessionConfigGroupID string

// SessionConfigID Unique identifier for a session configuration option.
type SessionConfigID string

// SessionConfigOption A session configuration option selector and its current state.
type SessionConfigOption struct {
	// Optional semantic category for this option (UX only).
	Category *SessionConfigOptionCategory `json:"category,omitempty"`
	// Optional description for the Client to display to the user.
	Description *string `json:"description,omitempty"`
	// Unique identifier for the configuration option.
	ID SessionConfigID `json:"id"`
	// Human-readable label for the option.
	Name string `json:"name"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
	// Single-value selector (dropdown).
	Select *SessionConfigSelect `json:"-"`
	// Boolean on/off toggle.
	Boolean *SessionConfigBoolean `json:"-"`
}

// MarshalJSON flattens whichever SessionConfigOption variant is set, plus the "type"
// discriminator, into one JSON object. It fails if zero or more than one
// variant is set.
func (v SessionConfigOption) MarshalJSON() ([]byte, error) {
	set := 0
	merged := map[string]json.RawMessage{}
	if v.Select != nil {
		set++
		raw, err := json.Marshal(v.Select)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("select")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.Boolean != nil {
		set++
		raw, err := json.Marshal(v.Boolean)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("boolean")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if set == 0 {
		return nil, fmt.Errorf("SessionConfigOption: no variant set")
	}
	if set > 1 {
		return nil, fmt.Errorf("SessionConfigOption: more than one variant set")
	}
	{
		fbytes, err := json.Marshal(v.Category)
		if err != nil {
			return nil, err
		}
		merged["category"] = fbytes
	}
	{
		fbytes, err := json.Marshal(v.Description)
		if err != nil {
			return nil, err
		}
		merged["description"] = fbytes
	}
	{
		fbytes, err := json.Marshal(v.ID)
		if err != nil {
			return nil, err
		}
		merged["id"] = fbytes
	}
	{
		fbytes, err := json.Marshal(v.Name)
		if err != nil {
			return nil, err
		}
		merged["name"] = fbytes
	}
	{
		fbytes, err := json.Marshal(v.Meta)
		if err != nil {
			return nil, err
		}
		merged["_meta"] = fbytes
	}
	return json.Marshal(merged)
}

// UnmarshalJSON dispatches on the "type" discriminator. A missing or
// unrecognized value is rejected.
func (v *SessionConfigOption) UnmarshalJSON(data []byte) error {
	var tag struct {
		Value *string `json:"type"`
	}
	if err := json.Unmarshal(data, &tag); err != nil {
		return fmt.Errorf("SessionConfigOption: %w", err)
	}
	*v = SessionConfigOption{}
	switch {
	case tag.Value != nil && *tag.Value == "select":
		var payload SessionConfigSelect
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionConfigOption: select: %w", err)
		}
		v.Select = &payload
	case tag.Value != nil && *tag.Value == "boolean":
		var payload SessionConfigBoolean
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionConfigOption: boolean: %w", err)
		}
		v.Boolean = &payload
	case tag.Value == nil:
		return fmt.Errorf("SessionConfigOption: missing discriminator field type")
	default:
		return fmt.Errorf("SessionConfigOption: unrecognized type %q", *tag.Value)
	}
	var base struct {
		Category    *SessionConfigOptionCategory `json:"category"`
		Description *string                      `json:"description"`
		ID          SessionConfigID              `json:"id"`
		Name        string                       `json:"name"`
		Meta        json.RawMessage              `json:"_meta"`
	}
	if err := json.Unmarshal(data, &base); err != nil {
		return fmt.Errorf("SessionConfigOption: %w", err)
	}
	v.Category = base.Category
	v.Description = base.Description
	v.ID = base.ID
	v.Name = base.Name
	v.Meta = base.Meta
	return nil
}

// SessionConfigOptionCategory Semantic category for a session configuration option.
//
// This is intended to help Clients distinguish broadly common selectors (e.g. model selector vs
// session mode selector vs thought/reasoning level) for UX purposes (keyboard shortcuts, icons,
// placement). It MUST NOT be required for correctness. Clients MUST handle missing or unknown
// categories gracefully.
//
// Category names beginning with `_` are free for custom use, like other ACP extension methods.
// Category names that do not begin with `_` are reserved for the ACP spec.
type SessionConfigOptionCategory string

const (
	// Session mode selector.
	SessionConfigOptionCategoryMode SessionConfigOptionCategory = "mode"
	// Model selector.
	SessionConfigOptionCategoryModel SessionConfigOptionCategory = "model"
	// Model-related configuration parameter.
	SessionConfigOptionCategoryModelConfig SessionConfigOptionCategory = "model_config"
	// Thought/reasoning level selector.
	SessionConfigOptionCategoryThoughtLevel SessionConfigOptionCategory = "thought_level"
)

// SessionConfigOptionsCapabilities Session configuration option capabilities supported by the client.
type SessionConfigOptionsCapabilities struct {
	// Whether the client supports boolean session configuration options.
	//
	// Optional. Omitted or `null` both mean the client does not advertise support.
	// Supplying `{}` means agents may include `type: "boolean"` entries in
	// `configOptions`, and the client may send `session/set_config_option`
	// requests with `type: "boolean"` and a boolean `value`.
	Boolean *BooleanConfigOptionCapabilities `json:"boolean,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionConfigSelect A single-value selector (dropdown) session configuration option payload.
type SessionConfigSelect struct {
	// The currently selected value.
	CurrentValue SessionConfigValueID `json:"currentValue"`
	// The set of selectable options.
	Options SessionConfigSelectOptions `json:"options"`
}

// SessionConfigSelectGroup A group of possible values for a session configuration option.
type SessionConfigSelectGroup struct {
	// Unique identifier for this group.
	Group SessionConfigGroupID `json:"group"`
	// Human-readable label for this group.
	Name string `json:"name"`
	// The set of option values in this group.
	Options []SessionConfigSelectOption `json:"options"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionConfigSelectOption A possible value for a session configuration option.
type SessionConfigSelectOption struct {
	// Optional description for this option value.
	Description *string `json:"description,omitempty"`
	// Human-readable label for this option value.
	Name string `json:"name"`
	// Unique identifier for this option value.
	Value SessionConfigValueID `json:"value"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionConfigSelectOptions Possible values for a session configuration option.
type SessionConfigSelectOptions struct {
	// A flat list of options with no grouping.
	Ungrouped []SessionConfigSelectOption `json:"-"`
	// A list of options grouped under headers.
	Grouped []SessionConfigSelectGroup `json:"-"`
}

// MarshalJSON marshals whichever SessionConfigSelectOptions variant is set. It fails if zero or
// more than one variant is set.
func (v SessionConfigSelectOptions) MarshalJSON() ([]byte, error) {
	set := 0
	var out []byte
	if v.Ungrouped != nil {
		set++
		var err error
		out, err = json.Marshal(v.Ungrouped)
		if err != nil {
			return nil, err
		}
	}
	if v.Grouped != nil {
		set++
		var err error
		out, err = json.Marshal(v.Grouped)
		if err != nil {
			return nil, err
		}
	}
	if set == 0 {
		return nil, fmt.Errorf("SessionConfigSelectOptions: no variant set")
	}
	if set > 1 {
		return nil, fmt.Errorf("SessionConfigSelectOptions: more than one variant set")
	}
	return out, nil
}

// UnmarshalJSON tells SessionConfigSelectOptions's variants apart by which one's required fields
// are present on the first array element (this union has no discriminator
// tag, and each variant is itself an array of a different element type).
func (v *SessionConfigSelectOptions) UnmarshalJSON(data []byte) error {
	var arr []json.RawMessage
	if err := json.Unmarshal(data, &arr); err != nil {
		return fmt.Errorf("SessionConfigSelectOptions: %w", err)
	}
	*v = SessionConfigSelectOptions{}
	if len(arr) == 0 {
		return fmt.Errorf("SessionConfigSelectOptions: empty array is ambiguous between variants")
	}
	var probe map[string]json.RawMessage
	if err := json.Unmarshal(arr[0], &probe); err != nil {
		return fmt.Errorf("SessionConfigSelectOptions: %w", err)
	}
	matches := 0
	if probe["name"] != nil && probe["value"] != nil {
		var payload []SessionConfigSelectOption
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionConfigSelectOptions: SessionConfigSelectOption: %w", err)
		}
		v.Ungrouped = payload
		matches++
	}
	if probe["group"] != nil && probe["name"] != nil && probe["options"] != nil {
		var payload []SessionConfigSelectGroup
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionConfigSelectOptions: SessionConfigSelectGroup: %w", err)
		}
		v.Grouped = payload
		matches++
	}
	if matches == 0 {
		return fmt.Errorf("SessionConfigSelectOptions: no variant's required fields are present")
	}
	if matches > 1 {
		return fmt.Errorf("SessionConfigSelectOptions: ambiguous, multiple variants match")
	}
	return nil
}

// SessionConfigValueID Unique identifier for a session configuration option value.
type SessionConfigValueID string

// SessionDeleteCapabilities Capabilities for the `session/delete` method.
//
// Supplying `{}` means the agent supports deleting sessions from `session/list`.
type SessionDeleteCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionID A unique identifier for a conversation session between a client and agent.
//
// Sessions maintain their own context, conversation history, and state,
// allowing multiple independent interactions with the same agent.
//
// See protocol docs: [Session ID](https://agentclientprotocol.com/protocol/session-setup#session-id)
type SessionID string

// SessionInfo Information about a session returned by session/list
type SessionInfo struct {
	// Additional workspace roots reported for this session. Each path must be absolute.
	//
	// When present, this is the complete ordered additional-root list reported
	// by the Agent. Omitted and empty values are equivalent: the response
	// reports no additional roots.
	AdditionalDirectories []string `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd string `json:"cwd"`
	// Unique identifier for the session
	SessionID SessionID `json:"sessionId"`
	// Human-readable title for the session
	Title *string `json:"title,omitempty"`
	// ISO 8601 timestamp of last activity
	UpdatedAt *string `json:"updatedAt,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionInfoUpdate Update to session metadata. All fields are optional to support partial updates.
//
// Agents send this notification to update session information like title or custom metadata.
// This allows clients to display dynamic session names and track session state changes.
type SessionInfoUpdate struct {
	// Human-readable title for the session. Set to null to clear.
	Title *string `json:"title,omitempty"`
	// ISO 8601 timestamp of last activity. Set to null to clear.
	UpdatedAt *string `json:"updatedAt,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionListCapabilities Capabilities for the `session/list` method.
//
// Supplying `{}` means the agent supports listing sessions.
type SessionListCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionMode A mode the agent can operate in.
//
// See protocol docs: [Session Modes](https://agentclientprotocol.com/protocol/session-modes)
type SessionMode struct {
	// Optional human-readable details shown with this protocol object.
	Description *string `json:"description,omitempty"`
	// Stable identifier used to refer to this protocol object in later messages.
	ID SessionModeID `json:"id"`
	// Human-readable name shown for this protocol object.
	Name string `json:"name"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionModeID Unique identifier for a Session Mode.
type SessionModeID string

// SessionModeState The set of modes and the one currently active.
type SessionModeState struct {
	// The set of modes that the Agent can operate in
	AvailableModes []SessionMode `json:"availableModes"`
	// The current mode the Agent is in.
	CurrentModeID SessionModeID `json:"currentModeId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionNotification Notification containing a session update from the agent.
//
// Used to stream real-time progress and results during prompt processing.
//
// See protocol docs: [Agent Reports Output](https://agentclientprotocol.com/protocol/prompt-turn#3-agent-reports-output)
type SessionNotification struct {
	// The ID of the session this update pertains to.
	SessionID SessionID `json:"sessionId"`
	// The actual update content.
	Update SessionUpdate `json:"update"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionResumeCapabilities Capabilities for the `session/resume` method.
//
// Supplying `{}` means the agent supports resuming sessions.
type SessionResumeCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SessionUpdate Different types of updates that can be sent during session processing.
//
// These updates provide real-time feedback about the agent's progress.
//
// See protocol docs: [Agent Reports Output](https://agentclientprotocol.com/protocol/prompt-turn#3-agent-reports-output)
type SessionUpdate struct {
	// A chunk of the user's message being streamed.
	UserMessageChunk *ContentChunk `json:"-"`
	// A chunk of the agent's response being streamed.
	AgentMessageChunk *ContentChunk `json:"-"`
	// A chunk of the agent's internal reasoning being streamed.
	AgentThoughtChunk *ContentChunk `json:"-"`
	// Notification that a new tool call has been initiated.
	ToolCall *ToolCall `json:"-"`
	// Update on the status or results of a tool call.
	ToolCallUpdate *ToolCallUpdate `json:"-"`
	// The agent's execution plan for complex tasks.
	// See protocol docs: [Agent Plan](https://agentclientprotocol.com/protocol/agent-plan)
	Plan *Plan `json:"-"`
	// Available commands are ready or have changed
	AvailableCommandsUpdate *AvailableCommandsUpdate `json:"-"`
	// The current mode of the session has changed
	//
	// See protocol docs: [Session Modes](https://agentclientprotocol.com/protocol/session-modes)
	CurrentModeUpdate *CurrentModeUpdate `json:"-"`
	// Session configuration options have been updated.
	ConfigOptionUpdate *ConfigOptionUpdate `json:"-"`
	// Session metadata has been updated (title, timestamps, custom metadata)
	SessionInfoUpdate *SessionInfoUpdate `json:"-"`
	// Context window and cost update for the session.
	UsageUpdate *UsageUpdate `json:"-"`
}

// MarshalJSON flattens whichever SessionUpdate variant is set, plus the "sessionUpdate"
// discriminator, into one JSON object. It fails if zero or more than one
// variant is set.
func (v SessionUpdate) MarshalJSON() ([]byte, error) {
	set := 0
	merged := map[string]json.RawMessage{}
	if v.UserMessageChunk != nil {
		set++
		raw, err := json.Marshal(v.UserMessageChunk)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("user_message_chunk")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.AgentMessageChunk != nil {
		set++
		raw, err := json.Marshal(v.AgentMessageChunk)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("agent_message_chunk")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.AgentThoughtChunk != nil {
		set++
		raw, err := json.Marshal(v.AgentThoughtChunk)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("agent_thought_chunk")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.ToolCall != nil {
		set++
		raw, err := json.Marshal(v.ToolCall)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("tool_call")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.ToolCallUpdate != nil {
		set++
		raw, err := json.Marshal(v.ToolCallUpdate)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("tool_call_update")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.Plan != nil {
		set++
		raw, err := json.Marshal(v.Plan)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("plan")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.AvailableCommandsUpdate != nil {
		set++
		raw, err := json.Marshal(v.AvailableCommandsUpdate)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("available_commands_update")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.CurrentModeUpdate != nil {
		set++
		raw, err := json.Marshal(v.CurrentModeUpdate)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("current_mode_update")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.ConfigOptionUpdate != nil {
		set++
		raw, err := json.Marshal(v.ConfigOptionUpdate)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("config_option_update")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.SessionInfoUpdate != nil {
		set++
		raw, err := json.Marshal(v.SessionInfoUpdate)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("session_info_update")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if v.UsageUpdate != nil {
		set++
		raw, err := json.Marshal(v.UsageUpdate)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("usage_update")
		if err != nil {
			return nil, err
		}
		merged["sessionUpdate"] = tagBytes
	}
	if set == 0 {
		return nil, fmt.Errorf("SessionUpdate: no variant set")
	}
	if set > 1 {
		return nil, fmt.Errorf("SessionUpdate: more than one variant set")
	}
	return json.Marshal(merged)
}

// UnmarshalJSON dispatches on the "sessionUpdate" discriminator. A missing or
// unrecognized value is rejected.
func (v *SessionUpdate) UnmarshalJSON(data []byte) error {
	var tag struct {
		Value *string `json:"sessionUpdate"`
	}
	if err := json.Unmarshal(data, &tag); err != nil {
		return fmt.Errorf("SessionUpdate: %w", err)
	}
	*v = SessionUpdate{}
	switch {
	case tag.Value != nil && *tag.Value == "user_message_chunk":
		var payload ContentChunk
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: user_message_chunk: %w", err)
		}
		v.UserMessageChunk = &payload
	case tag.Value != nil && *tag.Value == "agent_message_chunk":
		var payload ContentChunk
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: agent_message_chunk: %w", err)
		}
		v.AgentMessageChunk = &payload
	case tag.Value != nil && *tag.Value == "agent_thought_chunk":
		var payload ContentChunk
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: agent_thought_chunk: %w", err)
		}
		v.AgentThoughtChunk = &payload
	case tag.Value != nil && *tag.Value == "tool_call":
		var payload ToolCall
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: tool_call: %w", err)
		}
		v.ToolCall = &payload
	case tag.Value != nil && *tag.Value == "tool_call_update":
		var payload ToolCallUpdate
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: tool_call_update: %w", err)
		}
		v.ToolCallUpdate = &payload
	case tag.Value != nil && *tag.Value == "plan":
		var payload Plan
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: plan: %w", err)
		}
		v.Plan = &payload
	case tag.Value != nil && *tag.Value == "available_commands_update":
		var payload AvailableCommandsUpdate
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: available_commands_update: %w", err)
		}
		v.AvailableCommandsUpdate = &payload
	case tag.Value != nil && *tag.Value == "current_mode_update":
		var payload CurrentModeUpdate
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: current_mode_update: %w", err)
		}
		v.CurrentModeUpdate = &payload
	case tag.Value != nil && *tag.Value == "config_option_update":
		var payload ConfigOptionUpdate
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: config_option_update: %w", err)
		}
		v.ConfigOptionUpdate = &payload
	case tag.Value != nil && *tag.Value == "session_info_update":
		var payload SessionInfoUpdate
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: session_info_update: %w", err)
		}
		v.SessionInfoUpdate = &payload
	case tag.Value != nil && *tag.Value == "usage_update":
		var payload UsageUpdate
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("SessionUpdate: usage_update: %w", err)
		}
		v.UsageUpdate = &payload
	case tag.Value == nil:
		return fmt.Errorf("SessionUpdate: missing discriminator field sessionUpdate")
	default:
		return fmt.Errorf("SessionUpdate: unrecognized sessionUpdate %q", *tag.Value)
	}
	return nil
}

// SetSessionConfigOptionRequest Request parameters for setting a session configuration option.
type SetSessionConfigOptionRequest struct {
	// The ID of the configuration option to set.
	ConfigID SessionConfigID `json:"configId"`
	// The ID of the session to set the configuration option for.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
	// A boolean value (`type: "boolean"`).
	Boolean *bool `json:"-"`
	// A [`SessionConfigValueId`] string value.
	//
	// This is the default when `type` is absent on the wire. Unknown `type`
	// values with string payloads also gracefully deserialize into this
	// variant.
	ValueID *SessionConfigValueID `json:"-"`
}

// MarshalJSON flattens whichever SetSessionConfigOptionRequest variant is set, plus the "type"
// discriminator, into one JSON object. It fails if zero or more than one
// variant is set.
func (v SetSessionConfigOptionRequest) MarshalJSON() ([]byte, error) {
	set := 0
	merged := map[string]json.RawMessage{}
	if v.Boolean != nil {
		set++
		raw, err := json.Marshal(v.Boolean)
		if err != nil {
			return nil, err
		}
		merged["value"] = raw
		tagBytes, err := json.Marshal("boolean")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.ValueID != nil {
		set++
		raw, err := json.Marshal(v.ValueID)
		if err != nil {
			return nil, err
		}
		merged["value"] = raw
	}
	if set == 0 {
		return nil, fmt.Errorf("SetSessionConfigOptionRequest: no variant set")
	}
	if set > 1 {
		return nil, fmt.Errorf("SetSessionConfigOptionRequest: more than one variant set")
	}
	{
		fbytes, err := json.Marshal(v.ConfigID)
		if err != nil {
			return nil, err
		}
		merged["configId"] = fbytes
	}
	{
		fbytes, err := json.Marshal(v.SessionID)
		if err != nil {
			return nil, err
		}
		merged["sessionId"] = fbytes
	}
	{
		fbytes, err := json.Marshal(v.Meta)
		if err != nil {
			return nil, err
		}
		merged["_meta"] = fbytes
	}
	return json.Marshal(merged)
}

// UnmarshalJSON dispatches on the "type" discriminator. A missing or
// unrecognized value falls back to ValueID, matching the pinned schema.
func (v *SetSessionConfigOptionRequest) UnmarshalJSON(data []byte) error {
	var tag struct {
		Value *string `json:"type"`
	}
	if err := json.Unmarshal(data, &tag); err != nil {
		return fmt.Errorf("SetSessionConfigOptionRequest: %w", err)
	}
	*v = SetSessionConfigOptionRequest{}
	switch {
	case tag.Value != nil && *tag.Value == "boolean":
		var wrap struct {
			Value bool `json:"value"`
		}
		if err := json.Unmarshal(data, &wrap); err != nil {
			return fmt.Errorf("SetSessionConfigOptionRequest: boolean: %w", err)
		}
		v.Boolean = &wrap.Value
	default:
		var wrap struct {
			Value SessionConfigValueID `json:"value"`
		}
		if err := json.Unmarshal(data, &wrap); err != nil {
			return fmt.Errorf("SetSessionConfigOptionRequest: %w", err)
		}
		v.ValueID = &wrap.Value
	}
	var base struct {
		ConfigID  SessionConfigID `json:"configId"`
		SessionID SessionID       `json:"sessionId"`
		Meta      json.RawMessage `json:"_meta"`
	}
	if err := json.Unmarshal(data, &base); err != nil {
		return fmt.Errorf("SetSessionConfigOptionRequest: %w", err)
	}
	v.ConfigID = base.ConfigID
	v.SessionID = base.SessionID
	v.Meta = base.Meta
	return nil
}

// SetSessionConfigOptionResponse Response to `session/set_config_option` method.
type SetSessionConfigOptionResponse struct {
	// The full set of configuration options and their current values.
	ConfigOptions []SessionConfigOption `json:"configOptions"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SetSessionModeRequest Request parameters for setting a session mode.
type SetSessionModeRequest struct {
	// The ID of the mode to set.
	ModeID SessionModeID `json:"modeId"`
	// The ID of the session to set the mode for.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// SetSessionModeResponse Response to `session/set_mode` method.
type SetSessionModeResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// StopReason Reasons why an agent stops processing a prompt turn.
//
// See protocol docs: [Stop Reasons](https://agentclientprotocol.com/protocol/prompt-turn#stop-reasons)
type StopReason string

const (
	// The turn ended successfully.
	StopReasonEndTurn StopReason = "end_turn"
	// The turn ended because the agent reached the maximum number of tokens.
	StopReasonMaxTokens StopReason = "max_tokens"
	// The turn ended because the agent reached the maximum number of allowed
	// agent requests between user turns.
	StopReasonMaxTurnRequests StopReason = "max_turn_requests"
	// The turn ended because the agent refused to continue. The user prompt
	// and everything that comes after it won't be included in the next
	// prompt, so this should be reflected in the UI.
	StopReasonRefusal StopReason = "refusal"
	// The turn was cancelled by the client via `session/cancel`.
	//
	// This stop reason MUST be returned when the client sends a `session/cancel`
	// notification, even if the cancellation causes exceptions in underlying operations.
	// Agents should catch these exceptions and return this semantically meaningful
	// response to confirm successful cancellation.
	StopReasonCancelled StopReason = "cancelled"
)

// UnmarshalJSON rejects any StopReason value that is not one of the named
// StopReason constants: this enum is closed in the pinned schema.
func (v *StopReason) UnmarshalJSON(data []byte) error {
	var raw string
	if err := json.Unmarshal(data, &raw); err != nil {
		return fmt.Errorf("StopReason: %w", err)
	}
	switch StopReason(raw) {
	case StopReasonEndTurn, StopReasonMaxTokens, StopReasonMaxTurnRequests, StopReasonRefusal, StopReasonCancelled:
		*v = StopReason(raw)
		return nil
	default:
		return fmt.Errorf("StopReason: unrecognized value %v", raw)
	}
}

// Terminal Embed a terminal created with `terminal/create` by its id.
//
// The terminal must be added before calling `terminal/release`.
//
// See protocol docs: [Terminal](https://agentclientprotocol.com/protocol/terminals)
type Terminal struct {
	// Identifier of the terminal instance to embed in the content stream.
	TerminalID TerminalID `json:"terminalId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// TerminalExitStatus Exit status of a terminal command.
type TerminalExitStatus struct {
	// The process exit code (may be null if terminated by signal).
	ExitCode *uint32 `json:"exitCode,omitempty"`
	// The signal that terminated the process (may be null if exited normally).
	Signal *string `json:"signal,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// TerminalID Typed identifier used for terminal values on the wire.
type TerminalID string

// TerminalOutputRequest Request to get the current output and status of a terminal.
type TerminalOutputRequest struct {
	// The session ID for this request.
	SessionID SessionID `json:"sessionId"`
	// The ID of the terminal to get output from.
	TerminalID TerminalID `json:"terminalId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// TerminalOutputResponse Response containing the terminal output and exit status.
type TerminalOutputResponse struct {
	// Exit status if the command has completed.
	ExitStatus *TerminalExitStatus `json:"exitStatus,omitempty"`
	// The terminal output captured so far.
	Output string `json:"output"`
	// Whether the output was truncated due to byte limits.
	Truncated bool `json:"truncated"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// TextContent Text provided to or from an LLM.
type TextContent struct {
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Text payload carried by this content block.
	Text string `json:"text"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// TextResourceContents Text-based resource contents.
type TextResourceContents struct {
	// MIME type describing the encoded media payload.
	MimeType *string `json:"mimeType,omitempty"`
	// Text payload carried by this content block.
	Text string `json:"text"`
	// URI associated with this resource or media payload.
	URI string `json:"uri"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ToolCall Represents a tool call that the language model has requested.
//
// Tool calls are actions that the agent executes on behalf of the language model,
// such as reading files, executing code, or fetching data from external sources.
//
// See protocol docs: [Tool Calls](https://agentclientprotocol.com/protocol/tool-calls)
type ToolCall struct {
	// Content produced by the tool call.
	Content []ToolCallContent `json:"content,omitempty"`
	// The category of tool being invoked.
	// Helps clients choose appropriate icons and UI treatment.
	Kind *ToolKind `json:"kind,omitempty"`
	// File locations affected by this tool call.
	// Enables "follow-along" features in clients.
	Locations []ToolCallLocation `json:"locations,omitempty"`
	// Raw input parameters sent to the tool.
	RawInput json.RawMessage `json:"rawInput,omitempty"`
	// Raw output returned by the tool.
	RawOutput json.RawMessage `json:"rawOutput,omitempty"`
	// Current execution status of the tool call.
	Status *ToolCallStatus `json:"status,omitempty"`
	// Human-readable title describing what the tool is doing.
	Title string `json:"title"`
	// Unique identifier for this tool call within the session.
	ToolCallID ToolCallID `json:"toolCallId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ToolCallContent Content produced by a tool call.
//
// Tool calls can produce different types of content including
// standard content blocks (text, images) or file diffs.
//
// See protocol docs: [Content](https://agentclientprotocol.com/protocol/tool-calls#content)
type ToolCallContent struct {
	// Standard content block (text, images, resources).
	Content *Content `json:"-"`
	// File modification shown as a diff.
	Diff *Diff `json:"-"`
	// Embed a terminal created with `terminal/create` by its id.
	//
	// The terminal must be added before calling `terminal/release`.
	//
	// See protocol docs: [Terminal](https://agentclientprotocol.com/protocol/terminals)
	Terminal *Terminal `json:"-"`
}

// MarshalJSON flattens whichever ToolCallContent variant is set, plus the "type"
// discriminator, into one JSON object. It fails if zero or more than one
// variant is set.
func (v ToolCallContent) MarshalJSON() ([]byte, error) {
	set := 0
	merged := map[string]json.RawMessage{}
	if v.Content != nil {
		set++
		raw, err := json.Marshal(v.Content)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("content")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.Diff != nil {
		set++
		raw, err := json.Marshal(v.Diff)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("diff")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if v.Terminal != nil {
		set++
		raw, err := json.Marshal(v.Terminal)
		if err != nil {
			return nil, err
		}
		if err := json.Unmarshal(raw, &merged); err != nil {
			return nil, err
		}
		tagBytes, err := json.Marshal("terminal")
		if err != nil {
			return nil, err
		}
		merged["type"] = tagBytes
	}
	if set == 0 {
		return nil, fmt.Errorf("ToolCallContent: no variant set")
	}
	if set > 1 {
		return nil, fmt.Errorf("ToolCallContent: more than one variant set")
	}
	return json.Marshal(merged)
}

// UnmarshalJSON dispatches on the "type" discriminator. A missing or
// unrecognized value is rejected.
func (v *ToolCallContent) UnmarshalJSON(data []byte) error {
	var tag struct {
		Value *string `json:"type"`
	}
	if err := json.Unmarshal(data, &tag); err != nil {
		return fmt.Errorf("ToolCallContent: %w", err)
	}
	*v = ToolCallContent{}
	switch {
	case tag.Value != nil && *tag.Value == "content":
		var payload Content
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("ToolCallContent: content: %w", err)
		}
		v.Content = &payload
	case tag.Value != nil && *tag.Value == "diff":
		var payload Diff
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("ToolCallContent: diff: %w", err)
		}
		v.Diff = &payload
	case tag.Value != nil && *tag.Value == "terminal":
		var payload Terminal
		if err := json.Unmarshal(data, &payload); err != nil {
			return fmt.Errorf("ToolCallContent: terminal: %w", err)
		}
		v.Terminal = &payload
	case tag.Value == nil:
		return fmt.Errorf("ToolCallContent: missing discriminator field type")
	default:
		return fmt.Errorf("ToolCallContent: unrecognized type %q", *tag.Value)
	}
	return nil
}

// ToolCallID Unique identifier for a tool call within a session.
type ToolCallID string

// ToolCallLocation A file location being accessed or modified by a tool.
//
// Enables clients to implement "follow-along" features that track
// which files the agent is working with in real-time.
//
// See protocol docs: [Following the Agent](https://agentclientprotocol.com/protocol/tool-calls#following-the-agent)
type ToolCallLocation struct {
	// Optional line number within the file.
	Line *uint32 `json:"line,omitempty"`
	// The absolute file path being accessed or modified.
	Path string `json:"path"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ToolCallStatus Execution status of a tool call.
//
// Tool calls progress through different statuses during their lifecycle.
//
// See protocol docs: [Status](https://agentclientprotocol.com/protocol/tool-calls#status)
type ToolCallStatus string

const (
	// The tool call hasn't started running yet because the input is either
	// streaming or we're awaiting approval.
	ToolCallStatusPending ToolCallStatus = "pending"
	// The tool call is currently running.
	ToolCallStatusInProgress ToolCallStatus = "in_progress"
	// The tool call completed successfully.
	ToolCallStatusCompleted ToolCallStatus = "completed"
	// The tool call failed with an error.
	ToolCallStatusFailed ToolCallStatus = "failed"
)

// UnmarshalJSON rejects any ToolCallStatus value that is not one of the named
// ToolCallStatus constants: this enum is closed in the pinned schema.
func (v *ToolCallStatus) UnmarshalJSON(data []byte) error {
	var raw string
	if err := json.Unmarshal(data, &raw); err != nil {
		return fmt.Errorf("ToolCallStatus: %w", err)
	}
	switch ToolCallStatus(raw) {
	case ToolCallStatusPending, ToolCallStatusInProgress, ToolCallStatusCompleted, ToolCallStatusFailed:
		*v = ToolCallStatus(raw)
		return nil
	default:
		return fmt.Errorf("ToolCallStatus: unrecognized value %v", raw)
	}
}

// ToolCallUpdate An update to an existing tool call.
//
// Used to report progress and results as tools execute. All fields except
// the tool call ID are optional - only changed fields need to be included.
//
// See protocol docs: [Updating](https://agentclientprotocol.com/protocol/tool-calls#updating)
type ToolCallUpdate struct {
	// Replace the content collection.
	Content []ToolCallContent `json:"content,omitempty"`
	// Update the tool kind.
	Kind *ToolKind `json:"kind,omitempty"`
	// Replace the locations collection.
	Locations []ToolCallLocation `json:"locations,omitempty"`
	// Update the raw input.
	RawInput json.RawMessage `json:"rawInput,omitempty"`
	// Update the raw output.
	RawOutput json.RawMessage `json:"rawOutput,omitempty"`
	// Update the execution status.
	Status *ToolCallStatus `json:"status,omitempty"`
	// Update the human-readable title.
	Title *string `json:"title,omitempty"`
	// The ID of the tool call being updated.
	ToolCallID ToolCallID `json:"toolCallId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// ToolKind Categories of tools that can be invoked.
//
// Tool kinds help clients choose appropriate icons and optimize how they
// display tool execution progress.
//
// See protocol docs: [Creating](https://agentclientprotocol.com/protocol/tool-calls#creating)
type ToolKind string

const (
	// Reading files or data.
	ToolKindRead ToolKind = "read"
	// Modifying files or content.
	ToolKindEdit ToolKind = "edit"
	// Removing files or data.
	ToolKindDelete ToolKind = "delete"
	// Moving or renaming files.
	ToolKindMove ToolKind = "move"
	// Searching for information.
	ToolKindSearch ToolKind = "search"
	// Running commands or code.
	ToolKindExecute ToolKind = "execute"
	// Internal reasoning or planning.
	ToolKindThink ToolKind = "think"
	// Retrieving external data.
	ToolKindFetch ToolKind = "fetch"
	// Switching the current session mode.
	ToolKindSwitchMode ToolKind = "switch_mode"
	// Other tool types (default).
	ToolKindOther ToolKind = "other"
)

// UnmarshalJSON rejects any ToolKind value that is not one of the named
// ToolKind constants: this enum is closed in the pinned schema.
func (v *ToolKind) UnmarshalJSON(data []byte) error {
	var raw string
	if err := json.Unmarshal(data, &raw); err != nil {
		return fmt.Errorf("ToolKind: %w", err)
	}
	switch ToolKind(raw) {
	case ToolKindRead, ToolKindEdit, ToolKindDelete, ToolKindMove, ToolKindSearch, ToolKindExecute, ToolKindThink, ToolKindFetch, ToolKindSwitchMode, ToolKindOther:
		*v = ToolKind(raw)
		return nil
	default:
		return fmt.Errorf("ToolKind: unrecognized value %v", raw)
	}
}

// UnstructuredCommandInput All text that was typed after the command name is provided as input.
type UnstructuredCommandInput struct {
	// A hint to display when the input hasn't been provided yet
	Hint string `json:"hint"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// UsageUpdate Context window and cost update for a session.
type UsageUpdate struct {
	// Cumulative session cost (optional).
	Cost *Cost `json:"cost,omitempty"`
	// Total context window size in tokens.
	Size uint64 `json:"size"`
	// Tokens currently in context.
	Used uint64 `json:"used"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// WaitForTerminalExitRequest Request to wait for a terminal command to exit.
type WaitForTerminalExitRequest struct {
	// The session ID for this request.
	SessionID SessionID `json:"sessionId"`
	// The ID of the terminal to wait for.
	TerminalID TerminalID `json:"terminalId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// WaitForTerminalExitResponse Response containing the exit status of a terminal command.
type WaitForTerminalExitResponse struct {
	// The process exit code (may be null if terminated by signal).
	ExitCode *uint32 `json:"exitCode,omitempty"`
	// The signal that terminated the process (may be null if exited normally).
	Signal *string `json:"signal,omitempty"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// WriteTextFileRequest Request to write content to a text file.
//
// Only available if the client supports the `fs.writeTextFile` capability.
type WriteTextFileRequest struct {
	// The text content to write to the file.
	Content string `json:"content"`
	// Absolute path to the file to write.
	Path string `json:"path"`
	// The session ID for this request.
	SessionID SessionID `json:"sessionId"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}

// WriteTextFileResponse Response to `fs/write_text_file`
type WriteTextFileResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
	Meta json.RawMessage `json:"_meta,omitempty"`
}
